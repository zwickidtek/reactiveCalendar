const TaskCount= ({ count }) => {
    return(
        <div>
                    <div className="count"><span className="all-tasks">{count}</span> Tasks.</div>
        </div>
    )
}

export default TaskCount